# NOTE: This file is a template. Do not commit real secret values to git.
# Populate JWT_SECRET (and any other sensitive values) via your CI/CD pipeline,
# a sealed-secrets / external-secrets controller, or `kubectl create secret`.
apiVersion: v1
kind: Secret
metadata:
  name: backend-secret
  namespace: ai-task-platform
type: Opaque
stringData:
  JWT_SECRET: "REPLACE_WITH_REAL_SECRET_VIA_CI_OR_VAULT"
